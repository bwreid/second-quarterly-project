<p id="notice"><%= notice %></p>

<div class="row">
  <div class="large-6 columns large-centered">
    <p>
      <b>First:</b>
      <%= @user.first %>
    </p>

    <p>
      <b>Last:</b>
      <%= @user.last %>
    </p>

    <p>
      <b>Username:</b>
      <%= @user.username %>
    </p>

    <p>
      <b>Email:</b>
      <%= @user.email %>
    </p>

    <%= link_to 'Edit', edit_user_path(@user) %>
  </div>
</div>