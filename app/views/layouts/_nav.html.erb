<nav class="top-bar">
  <ul class="title-area">
    <!-- Title Area -->
    <li class="name">
      <h1 class=""><%= link_to("akio", welcome_index_path) %></h1>
    </li>
    <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
    <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
  </ul>

  <section class="top-bar-section">
    <!-- Left Nav Section -->
    <ul class="left">
      <!--<li class="divider"></li>
      <li><%#= link_to("Graphs", '#') %>
      <li class="divider"></li> -->
    </ul>

    <!-- Right Nav Section -->
    <ul class="right">
      <li class="divider"></li>
      <% if @auth.present? %>
      <!-- <li><%#= link_to("Give Me Something New", scrape_jobs_path, :method => 'post', :remote => true ) %></li> -->
      <li class="divider"></li>
      <li><%= link_to("My Account", @auth) %></li>
      <li class="divider"></li>
      <li><%= link_to("Logout", login_path, :method => 'delete') %></li>
      <li class="divider"></li>
      <% else %>
      <li><%= link_to("Create Account", new_user_path) %></li>
      <li class="divider"></li>
      <li><%= link_to("Login", login_path) %></li>
      <li class="divider"></li>
      <% end %>
    </ul>
  </section>
</nav>